# Claude memory — OUR TIME

These are the two notes Claude Code keeps about this project between sessions.
They are copied here so the repo is self-contained: hand this repo to another
Claude session (or another developer) and it has the context.

- **`our-time-website.md`** — the project note. Brand facts, the OVERTIME→OUR TIME
  rename, decisions taken, the image pipeline, and the hard technical constraints.
- **`gemini-image-editing.md`** — the reusable technique for reference-conditioned
  image editing, and the two failure modes that bite every time.

> **Only these two files are included.** The full memory directory holds notes on
> unrelated projects and other people, so it is deliberately not in this repo.
